# Smart Workforce Analytics & Management System

This project stores employee data in SQLite, analyzes it using Pandas and NumPy, creates charts with Matplotlib, and presents everything through a Flask dashboard.

## Run

```bash
python main.py
python app.py
```

Open `http://127.0.0.1:5000/` for the web app.
